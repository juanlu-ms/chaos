# Bloquea la instalación de paquetes publicados hace menos de 24 horas
# (Ajusta el tiempo según tu nivel de paranoia)
pnpm.minimum-package-age=86400000

# Evita que se ejecuten scripts de instalación automáticamente
ignore-scripts=true
